{
  "name": "platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint . --max-warnings 0",
    "preview": "vite preview"
  },
  "dependencies": {
    "@repo/config": "*",
    "@repo/typescript-config": "*",
    "@repo/store": "*",
    "@repo/ui": "*"
  }
}
